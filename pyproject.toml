[project]
name = "just-finish-it"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=3.8.0",
    "python-dotenv>=1.2.3",
    "rich>=15.0.0",
]
